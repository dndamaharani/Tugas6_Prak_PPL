<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Pengeluaran - Expense Tracker</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-8">
    <div class="max-w-2xl mx-auto">
        <!-- Header -->
        <div class="flex items-center justify-between mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-900">Detail Pengeluaran</h1>
                <p class="text-gray-600 mt-2">Informasi lengkap pengeluaran</p>
            </div>
            <a href="/expenses" class="text-blue-600 hover:text-blue-800 font-medium">
                ← Kembali ke Dashboard
            </a>
        </div>

        <!-- Detail Card -->
        <div class="bg-white shadow-lg rounded-lg overflow-hidden">
            <div class="px-6 py-4 bg-gray-50 border-b">
                <h2 class="text-xl font-semibold text-gray-900">{{ expense.title }}</h2>
            </div>
            
            <div class="p-6 space-y-4">
                <!-- Jumlah -->
                <div class="flex justify-between items-center py-3 border-b">
                    <span class="text-gray-600 font-medium">Jumlah:</span>
                    <span class="text-2xl font-bold text-red-600">-Rp {{ Number(expense.amount).toLocaleString('id-ID', { minimumFractionDigits: 0, maximumFractionDigits: 0 }) }}</span>
                </div>

                <!-- Kategori -->
                <div class="flex justify-between items-center py-3 border-b">
                    <span class="text-gray-600 font-medium">Kategori:</span>
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-blue-100 text-blue-800">
                        {{ expense.category }}
                    </span>
                </div>

                <!-- Tanggal -->
                <div class="flex justify-between items-center py-3 border-b">
                    <span class="text-gray-600 font-medium">Tanggal:</span>
                    <span class="text-gray-900">{{ expense.expenseDate.toFormat('dd MMMM yyyy') }}</span>
                </div>

                <!-- Deskripsi -->
                @if(expense.description)
                <div class="py-3 border-b">
                    <span class="text-gray-600 font-medium block mb-2">Deskripsi:</span>
                    <p class="text-gray-900 bg-gray-50 p-3 rounded-md">{{ expense.description }}</p>
                </div>
                @endif

                <!-- Waktu Dibuat -->
                <div class="flex justify-between items-center py-3 text-sm text-gray-500">
                    <span>Dicatat pada:</span>
                    <span>{{ expense.createdAt.toFormat('dd MMMM yyyy, HH:mm') }}</span>
                </div>
            </div>
        </div>
    </div>
</body>
</html>